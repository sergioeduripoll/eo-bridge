# Core
Flask==3.0.3
gunicorn==22.0.0

# CRÍTICO: websocket-client DEBE instalarse ANTES de ExpertOptionAPI
# para evitar conflicto con el paquete "websocket" (sin -client)
websocket-client==1.7.0

# La API de ExpertOption
ExpertOptionAPI

# Dependencias explícitas que ExpertOptionAPI necesita
requests>=2.31.0
simplejson
